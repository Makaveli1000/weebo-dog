[build]
  # CRITICAL: These values MUST be enclosed in double quotes for valid TOML syntax.
  command = "npm run build"
  publish = "dist" 
  
  functions = "netlify/functions"
  
  environment = {
    SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
  }