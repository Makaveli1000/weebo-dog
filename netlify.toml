# netlify.toml

[build]
  # CRITICAL FIX: Explicitly call bash to run the script.
  # This fixes the "Permission denied" error (Exit Code 126).
  command = "bash ./create-config.sh && npm install" 
  
  publish = "/" 
  functions = "netlify/functions"
  
# ... (rest of your configuration should remain as previously corrected)
