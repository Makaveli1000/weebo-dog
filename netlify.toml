[build]
  # CRITICAL: These values MUST be enclosed in double quotes for valid TOML syntax.
  command = "npm run build"
  publish = "dist" 
  
  # FIX: The value for 'functions' must also be a quoted string.
  functions = "netlify/functions" # <<<--- THIS IS THE FIX
  
  environment = {
    SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
  }