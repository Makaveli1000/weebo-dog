# 1. Standard Ignores
node_modules/
.firebase/
.vscode/
*.log
.DS_Store

# 2. THE KEY PROTECTION (Global & Specific)
# This ensures your keys are never tracked, no matter where they are
env-config.js
dist/env-config.js
.env
.env.local
sntlmoexclusivesportsgrid-firebase-adminsdk-*.json

# 3. THE DIST STRATEGY
# First, tell git NOT to ignore the dist folder itself
!dist/

# Now, ignore the specific key file inside dist (redundant but safe)
dist/env-config.js

# Allow everything else in dist (like bundle.js, styles, etc.)
!dist/*