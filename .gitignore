# 1. Standard Ignores
node_modules/
.firebase/
.vscode/
*.log
.DS_Store

# 2. THE KEY PROTECTION (Global & Specific)
# This ensures your keys are never tracked, no matter where they are
env-config.js
dist/env-config.js
.env
.env.local
sntlmoexclusivesportsgrid-firebase-adminsdk-*.json

# 3. THE DIST STRATEGY
# Tell git to track the folder
!dist/

# Specifically ignore the sensitive keys inside dist
dist/env-config.js
dist/sntlmoexclusivesportsgrid-firebase-adminsdk-*.json

# DO NOT ignore these - they must stay in the cloud!
!dist/index.html
!dist/bundle.js
!dist/output.css
!dist/favicon.ico